"""
mana-habilidade-placeholder — habilidade da Maná Builder (template).

Após "Use this template", renomeie:
1. A pasta `src/mana_habilidade_placeholder/` para `src/mana_habilidade_<nome>/`
   (lowercase, underscore — Python module convention).
2. O nome no `pyproject.toml` para `mana-habilidade-<nome>` (lowercase, hifenizado).
3. Os imports nos testes para `mana_habilidade_<nome>`.
"""

__version__ = "0.1.0"

# Re-exportar a API pública aqui (substituir após implementação).
# Exemplo:
# from .core import SuaClasse, sua_funcao
#
# __all__ = ["SuaClasse", "sua_funcao"]

__all__: list[str] = []
