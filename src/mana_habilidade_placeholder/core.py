"""
Implementação principal da habilidade.

Substituir o placeholder abaixo pela implementação real após "Use this template".
Manter funções pequenas, com type hints e docstrings.
"""

from __future__ import annotations


def placeholder_function(entrada: str) -> str:
    """
    Função placeholder de exemplo. Substituir pela implementação real.

    Args:
        entrada: descrever o parâmetro de entrada.

    Returns:
        Descrever o retorno.

    Raises:
        ValueError: quando a entrada é inválida (substituir pelas reais).

    Example:
        >>> placeholder_function("teste")
        'teste processado'
    """
    if not entrada:
        raise ValueError("Entrada não pode ser vazia")
    return f"{entrada} processado"
